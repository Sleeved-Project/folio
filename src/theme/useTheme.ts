import { theme as defaultTheme } from './theme';

export const useTheme = () => {
  // Here we can add logic to retrieve the theme from a context or other state management solution
  // For now, we will return the default theme directly
  return defaultTheme;
};
