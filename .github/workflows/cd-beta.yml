name: EAS Update

on:
  release:
    types: [published]

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: npm

      - name: 🏗 Setup EAS CLI
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔐 Login to EAS
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          npx eas whoami

      - name: 🚀 Publish update
        env:
          EXPO_PUBLIC_API_ATLAS_BASE_URL: ${{ secrets.EXPO_PUBLIC_API_ATLAS_BASE_URL }}
          EXPO_PUBLIC_API_WARDEN_BASE_URL: ${{ secrets.EXPO_PUBLIC_API_WARDEN_BASE_URL }}
        run: |
          npx eas update --branch=release --message="Release ${{ github.ref_name }}"
